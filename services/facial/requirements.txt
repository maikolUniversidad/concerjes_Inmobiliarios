# Servicio facial — Conserjes Inmobiliarios
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4

# Detección (SCRFD) + embedding ArcFace 512-d (buffalo_l)
insightface==0.7.3
# GPU: onnxruntime-gpu · CPU (pruebas): cambiar por onnxruntime
onnxruntime-gpu==1.20.1

opencv-python-headless==4.10.0.84
numpy==1.26.4

# Anti-spoofing pasivo (MiniFASNet). Arrastra torch; descarga pesos solo.
# Si no quieres anti-spoofing, quita esta línea y usa LIVENESS_MODE=disabled
# (SOLO para pruebas: sin liveness, una foto impresa puede engañar al sistema).
deepface==0.0.93
tf-keras==2.18.0
