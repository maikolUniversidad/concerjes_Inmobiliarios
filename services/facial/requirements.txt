# Servicio facial — Conserjes Inmobiliarios
# Todas con wheels manylinux: instalan sin compilar (clave para Vercel).
# NO incluye insightface ni torch a propósito: no caben en serverless.
fastapi==0.115.6
pydantic==2.10.4

# Inferencia ONNX en CPU (~50 MB)
onnxruntime==1.20.1

# YuNet (detección + 5 puntos) viene dentro de OpenCV
opencv-python-headless==4.10.0.84
numpy==1.26.4

# Solo on-premise (Docker): descomenta para anti-spoofing MiniFASNet real.
# Arrastra torch (~800 MB) → NO cabe en Vercel. Requiere LIVENESS_MODE=fasnet.
# deepface==0.0.93
# tf-keras==2.18.0
